version: 2.1

orbs:
  newspack: newspack/newspack@1.5.8

workflows:
  version: 2
  all:
    jobs:
      - newspack/build
      - newspack/i18n:
          requires:
            - newspack/build
          filters:
            branches:
              only:
                - trunk
      - newspack/lint-js-scss:
          requires:
            - newspack/build
      - newspack/test-js:
          requires:
            - newspack/build
      - newspack/build-distributable:
          requires:
            - newspack/build
          archive-name: 'newspack-story-budget'
      - newspack/release:
          requires:
            - newspack/build
          filters:
            branches:
              only:
                - release
                - alpha
                - /^hotfix\/.*/
                - /^epic\/.*/
      - newspack/post-release:
          requires:
            - newspack/release
          filters:
            branches:
              only:
                - release
  php:
    jobs:
      - newspack/lint-php
      - newspack/test-php
