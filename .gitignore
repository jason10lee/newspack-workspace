.env
# Standalone/local plugin & theme checkouts outside the monorepo (see AGENTS.md);
# mounted at /newspack-repos and symlinked into the active site. The
# repos/{plugins,themes} dirs are tracked (.gitkeep); their contents are not.
repos/plugins/*
repos/themes/*
repos/*
!repos/plugins/.gitkeep
!repos/themes/.gitkeep
logs
html
# Anchored: this is the workspace-root database volume. Unanchored it also
# swallowed source directories named `data/` inside plugins (e.g. a wizard's
# data hooks), silently dropping new files there from commits.
/data
bin/secrets.json
bin/repos.local.sh
snapshots
manager-html
additional-sites-html
bin/manager.private.key
bin/manager.public.key
.DS_Store
bin/newspack-network/nodes-keys.json
bin/newspack-network/service-accounts.secret
rootCA.pem
worktrees
worktrees-repos
envs
docker-compose.env-*.yml
docker-compose.override.yml
.claude/settings.local.json
node_modules/
.pnpm-store/
vendor/
composer.lock
packages/*/dist/
packages/*/build/
packages/components/shared/
packages/components/colors/
plugins/*/package-lock.json
themes/*/package-lock.json
packages/*/package-lock.json
.cursorignore
# Per-checkout personal hooks run by .husky/pre-commit (see AGENTS.md).
# `*.local*` also covers editor backups like pre-commit.local~ / .local.bak.
.husky/*.local*
.gstack/

# Security scan artifacts: findings, patches, and reports from AI security
# scanners. This repo is public, and deleting a published commit does not
# undelete it, so the cost of missing one is not symmetric with the cost of an
# over-wide pattern that silently drops source files.
#
# Named rather than globbed, for the reason the `/data` block above records:
# a broad pattern swallows source directories. These names are specific enough
# to need no anchor, so unlike `/data` they match at any depth — which is what
# catches a scan started from inside a plugin directory.
#
# `CLAUDE-SECURITY-*` is deliberate belt-and-braces: that scanner fences its own
# report directory with a `*` .gitignore, so this line is the second layer, and
# it still covers a run killed before the fence lands. `.codex-security/` has no
# such fence and this line is its only protection. When adding a scanner, that
# is the question to ask — does it fence its own output?
CLAUDE-SECURITY-*
.codex-security/

# PHPUnit's per-run defect/timing cache. Written wherever tests are run, so it
# is ignored workspace-wide rather than per package.
.phpunit.result.cache
